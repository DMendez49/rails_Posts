<h1> Pages </h1>
<p><%= link_to 'New Note', new_page_path %></p>
<% @pages.each do |page| %>
  <h2><%= page.title %></h2>
  <%= link_to 'Show Note', page_path(page) %>
  <%= link_to 'Edit Note', edit_page_path(page) %>
  <%= link_to 'Desynchronize Note', page, method: :delete %>
<% end %>





